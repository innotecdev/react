import React from 'react'

/*export default function() {
    return <h1>Retorno do Componente</h1>
}*/
/*
export default (props) => (
    <h1>{props.value}</h1>
)*/

export default props => (
    <h1>{props.value}</h1>
)